[package]
name = "shard"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
yaml_serde = "0.10"

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
